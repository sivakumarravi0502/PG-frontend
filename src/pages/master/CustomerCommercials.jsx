import CommercialsPage from './CommercialsPage';

export default function CustomerCommercials() {
  return (
    <CommercialsPage
      endpoint="customer"
      title="Customer Commercials"
      subtitle="Bundled rate card (PG MDR + margin) charged to Normal Customers — auto-fills the % field in Data Entry"
    />
  );
}
